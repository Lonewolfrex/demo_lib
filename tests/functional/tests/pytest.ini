[tool:pytest]
addopts = -v -s -n auto --html=reports/report.html --self-contained-html --alluredir=reports/allure-results
testpaths = tests
pythonpath = tests/functional_tests
testpaths = tests/functional_tests
python_files = test_*.py
markers =
    browser: chrome/firefox/edge
    smoke: Critical path tests
selenium_capture_debug = failure
